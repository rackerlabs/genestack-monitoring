additionalPrometheusRulesMap:
  instancenode-num-cpusum:
    groups:
      - name: node-exporter-rules
        rules:
          - expr: count without (cpu, mode) ( node_cpu_seconds_total{job="node-exporter",mode="idle"} )
            record: instance:node_num_cpu:sum
