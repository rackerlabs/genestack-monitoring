groups:
- name: mariadb-alerts
  rules:
  - alert: MysqlSlowQueries
    annotations:
      description: 'MySQL server has some new slow queries.

        VALUE = {{ $value }}

        LABELS = {{ $labels }}

        '
      summary: MySQL slow queries (instance {{ $labels.instance }})
    expr: increase(mysql_global_status_slow_queries[1m]) > 0
    for: 2m
    labels:
      severity: warning
