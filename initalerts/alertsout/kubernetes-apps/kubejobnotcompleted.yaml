groups:
- name: kubernetes-apps
  rules:
  - alert: KubeJobNotCompleted
    annotations:
      description: Job {{ $labels.namespace }}/{{ $labels.job_name }} is taking more
        than {{ "43200" | humanizeDuration }} to complete on cluster {{ $labels.cluster
        }}.
      runbook_url: https://runbooks.prometheus-operator.dev/runbooks/kubernetes/kubejobnotcompleted
      summary: Job did not complete in time
    expr: "time() - max by (namespace, job_name, cluster) (kube_job_status_start_time{job=\"\
      kube-state-metrics\", namespace=~\".*\"}\n  and\nkube_job_status_active{job=\"\
      kube-state-metrics\", namespace=~\".*\"} > 0) > 43200"
    labels:
      rackspace_com_coreAccountID: '5002029'
      rackspace_com_overseerID: '935811'
      severity: warning
