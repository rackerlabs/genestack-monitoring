additionalPrometheusRulesMap:
  kube-memory-overcommit:
    groups:
      - name: kube-memory-overcommit
        rules:
          # NOTE: kube_node_status_allocatable and kube_node_role may not be available in OTel k8sclusterreceiver
          # Consider using Prometheus receiver for kube-state-metrics or recording rules
          - alert: kube-memory-overcommit-warning
            expr: >-
              # Non-HA clusters. ( ( sum by (cluster) (namespace_memory:kube_pod_container_resource_requests:sum{}) - sum by (cluster) (kube_node_status_allocatable{job="kube-state-metrics",resource="memory"}) > 0 )

              and count by (cluster) (max by (cluster, node) (kube_node_role{job="kube-state-metrics", role="control-plane"})) < 3 )

              or # HA clusters. ( sum by (cluster) (namespace_memory:kube_pod_container_resource_requests:sum{}) - ( # Skip clusters with only one allocatable node. ( sum by (cluster) (kube_node_status_allocatable{job="kube-state-metrics",resource="memory"}) - max by (cluster) (kube_node_status_allocatable{job="kube-state-metrics",resource="memory"}) ) > 0 ) > 0 )
            for: 5m
            labels:
              severity: warning
              group_name: kube-memory-overcommit
              node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
            annotations:
              description: Cluster {{ $labels.cluster }} has overcommitted memory resource requests for Pods by {{ $value | humanize }} bytes and cannot tolerate node failure.
              runbook_url: default_runbook_url
              summary: Cluster has overcommitted memory resource requests.
              dashboard_url: default_dashboard_url
          # NOTE: kube_node_status_allocatable and kube_node_role may not be available in OTel k8sclusterreceiver
          # Consider using Prometheus receiver for kube-state-metrics or recording rules
          - alert: kube-memory-overcommit-critical
            expr: >-
              # Non-HA clusters. ( ( sum by (cluster) (namespace_memory:kube_pod_container_resource_requests:sum{}) - sum by (cluster) (kube_node_status_allocatable{job="kube-state-metrics",resource="memory"}) > 0 )

              and count by (cluster) (max by (cluster, node) (kube_node_role{job="kube-state-metrics", role="control-plane"})) < 3 )

              or # HA clusters. ( sum by (cluster) (namespace_memory:kube_pod_container_resource_requests:sum{}) - ( # Skip clusters with only one allocatable node. ( sum by (cluster) (kube_node_status_allocatable{job="kube-state-metrics",resource="memory"}) - max by (cluster) (kube_node_status_allocatable{job="kube-state-metrics",resource="memory"}) ) > 0 ) > 0 )
            for: 15m
            labels:
              severity: critical
              group_name: kube-memory-overcommit
              node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
            annotations:
              description: Cluster {{ $labels.cluster }} has overcommitted memory resource requests for Pods by {{ $value | humanize }} bytes and cannot tolerate node failure.
              runbook_url: default_runbook_url
              summary: Cluster has overcommitted memory resource requests.
              dashboard_url: default_dashboard_url
