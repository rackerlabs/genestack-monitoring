additionalPrometheusRulesMap:
  kube-node-unreachable:
    groups:
      - name: kube-node-unreachable
        rules:
          # NOTE: kube_node_spec_taint may not be available in OTel k8sclusterreceiver
          # Consider using Prometheus receiver for kube-state-metrics
          - alert: kube-node-unreachable-warning
            expr: >-
              (kube_node_spec_taint{job="kube-state-metrics",key="node.kubernetes.io/unreachable",effect="NoSchedule"} unless ignoring(key,value) kube_node_spec_taint{job="kube-state-metrics",key=~"ToBeDeletedByClusterAutoscaler|cloud.google.com/impending-node-termination|aws-node-termination-handler/spot-itn"}) == 1
            for: 5m
            labels:
              severity: warning
              group_name: kube-node-unreachable
              node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
            annotations:
              description: "{{ $labels.node }} is unreachable and some workloads may be rescheduled on cluster {{ $labels.cluster }}."
              runbook_url: default_runbook_url
              summary: Node is unreachable.
              dashboard_url: default_dashboard_url
          # NOTE: kube_node_spec_taint may not be available in OTel k8sclusterreceiver
          # Consider using Prometheus receiver for kube-state-metrics
          - alert: kube-node-unreachable-critical
            expr: >-
              (kube_node_spec_taint{job="kube-state-metrics",key="node.kubernetes.io/unreachable",effect="NoSchedule"} unless ignoring(key,value) kube_node_spec_taint{job="kube-state-metrics",key=~"ToBeDeletedByClusterAutoscaler|cloud.google.com/impending-node-termination|aws-node-termination-handler/spot-itn"}) == 1
            for: 15m
            labels:
              severity: critical
              group_name: kube-node-unreachable
              node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
            annotations:
              description: "{{ $labels.node }} is unreachable and some workloads may be rescheduled on cluster {{ $labels.cluster }}."
              runbook_url: default_runbook_url
              summary: Node is unreachable.
              dashboard_url: default_dashboard_url
