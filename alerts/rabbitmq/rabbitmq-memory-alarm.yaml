additionalPrometheusRulesMap:
  rabbitmq-memory-alarm:
    groups:
    - name: rabbitmq-memory-alarm
      rules:
      - alert: rabbitmq-memory-alarm-warning
        expr: max by(rabbitmq_cluster) (max_over_time(rabbitmq_alarms_memory_used_watermark[5m])  * on(instance) group_left(rabbitmq_cluster) max by (instance, rabbitmq_cluster) (rabbitmq_identity_info)) > 0
        for: 0m
        labels:
          severity: warning
          group_name: rabbitmq-memory-alarm
          node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
        annotations:
          description: >-
            RabbitMQ cluster `{{ $labels.rabbitmq_cluster }}` memory alarm active. Publishers are blocked.
            This warning alert fires immediately.
          runbook_url: default_runbook_url
          summary: 'A RabbitMQ node reached the `vm_memory_high_watermark` threshold.

            See https://www.rabbitmq.com/docs/alarms#overview, https://www.rabbitmq.com/docs/memory.

            '
          dashboard_url: default_dashboard_url
      - alert: rabbitmq-memory-alarm-critical
        expr: max by(rabbitmq_cluster) (max_over_time(rabbitmq_alarms_memory_used_watermark[5m])  * on(instance) group_left(rabbitmq_cluster) max by (instance, rabbitmq_cluster) (rabbitmq_identity_info)) > 0
        for: 5m
        labels:
          severity: critical
          group_name: rabbitmq-memory-alarm
          node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
        annotations:
          description: >-
            RabbitMQ cluster `{{ $labels.rabbitmq_cluster }}` memory alarm active. Publishers are blocked.
            This critical alert fires after 5m.
          runbook_url: default_runbook_url
          summary: 'A RabbitMQ node reached the `vm_memory_high_watermark` threshold.

            See https://www.rabbitmq.com/docs/alarms#overview, https://www.rabbitmq.com/docs/memory.

            '
          dashboard_url: default_dashboard_url
