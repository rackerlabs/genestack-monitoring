additionalPrometheusRulesMap:
  openstack-api-tls-certificate-expiring:
    groups:
    - name: openstack-api-tls-certificate-expiring
      rules:
      - alert: openstack-api-tls-certificate-expiring-warning
        expr: (time() - httpcheck_tls_cert_remaining_seconds) < 2160000
        for: 5m
        labels:
          severity: warning
          group_name: openstack-api-tls-certificate-expiring
          node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
        annotations:
          description: >-
            SSL certificate expires within 25 days. This warning alert fires immediately.
          runbook_url: default_runbook_url
          summary: SSL certificate will expire soon on (instance {{ $labels.http_url }})
          dashboard_url: default_dashboard_url
      - alert: openstack-api-tls-certificate-expiring-critical
        expr: (time() - httpcheck_tls_cert_remaining_seconds) < 604800
        for: 5m
        labels:
          severity: critical
          group_name: openstack-api-tls-certificate-expiring
          node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
        annotations:
          description: >-
            SSL certificate expires within 7 days. This critical alert fires after 5m.
          runbook_url: default_runbook_url
          summary: SSL certificate will expire soon on (instance {{ $labels.http_url }})
          dashboard_url: default_dashboard_url
