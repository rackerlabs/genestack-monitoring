additionalPrometheusRulesMap:
  prometheus-operator-list-errors:
    groups:
    - name: prometheus-operator-list-errors
      rules:
      - alert: prometheus-operator-list-errors-warning
        expr: (sum by (cluster,controller,namespace) (rate(prometheus_operator_list_operations_failed_total[10m])) / sum by (cluster,controller,namespace) (rate(prometheus_operator_list_operations_total[10m]))) > 0.4
        for: 15m
        labels:
          severity: warning
          group_name: prometheus-operator-list-errors
          node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
        annotations:
          description: >-
            Errors while performing List operations in controller {{$labels.controller}} in
            {{$labels.namespace}} namespace. This warning alert fires after 15m.
          runbook_url: default_runbook_url
          summary: Errors while performing list operations in controller.
          dashboard_url: default_dashboard_url
      - alert: prometheus-operator-list-errors-critical
        expr: (sum by (cluster,controller,namespace) (rate(prometheus_operator_list_operations_failed_total[10m])) / sum by (cluster,controller,namespace) (rate(prometheus_operator_list_operations_total[10m]))) > 0.4
        for: 30m
        labels:
          severity: critical
          group_name: prometheus-operator-list-errors
          node_hostname: '{{ reReplaceAll ":.*" "" $labels.instance }}'
        annotations:
          description: >-
            Errors while performing List operations in controller {{$labels.controller}} in
            {{$labels.namespace}} namespace. This critical alert fires after 30m.
          runbook_url: default_runbook_url
          summary: Errors while performing list operations in controller.
          dashboard_url: default_dashboard_url
